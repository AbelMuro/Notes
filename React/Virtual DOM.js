/* 
                    VIRTUAL DOM

      The virtual DOM is an exact copy of the REAL DOM, but it is used by React developers to 'mutate' the real DOM 
      in the most efficient way possible. As React developers, we don't actually work with the Real DOM, we make changes
      to the virtual DOM and React will efficiently update the real DOM using reconciliation
        

*/
