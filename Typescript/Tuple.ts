/* 
                TUPLE

        You can use a tuple to statically type an array
*/

type x = [string, number]              // this data type will accept an array with a string 
                                       // as the first value and a number as the second value
